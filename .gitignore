# Raw IBTrACS data — build inputs only. Too big for GitHub (100 MB/file limit) and not
# needed on the web (the site serves the small data/ files, generated by process_storms.py).
*.csv
*.csv.tmp
*.tmp

# Apache-only; ignored by GitHub Pages
.htaccess

# macOS / editor junk
.DS_Store
**/.DS_Store
Icon?
._*
